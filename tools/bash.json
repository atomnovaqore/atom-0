{
  "type": "function",
  "function": {
    "name": "bash",
    "description": "Execute a bash command on the system and return its output. Use this to run shell commands, inspect files, check system state, install packages, or perform any operation available via the command line.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The bash command to execute"
        }
      },
      "required": ["command"]
    }
  }
}
